import { create, NButton, NSpace, NLayout, NLayoutHeader,
  NLayoutContent, NLayoutFooter, NLayoutSider, NImage, NGrid, NGi, NH1,
  NText, NForm, NFormItem, NInput, NMessageProvider, NIcon, NAvatar, NEllipsis,
  NTabs, NTabPane, NModal, NCard
} from "naive-ui"

const naive = create({
  components: [NButton, NSpace, NLayout, NLayoutHeader,
    NLayoutContent, NLayoutFooter, NLayoutSider, 
    NImage, NGrid, NGi, NH1, NText,
    NForm, NFormItem, NInput, NMessageProvider, NIcon, NAvatar, NEllipsis,
    NTabs, NTabPane, NModal, NCard
  ]
})

export default naive
